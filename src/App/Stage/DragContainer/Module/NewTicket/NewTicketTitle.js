import styled from "styled-components";

const NewTicketTitle = styled.input`
  position: relative;
  width: 100%;
  padding: 0;
  margin: 0;
  border: none;
  color: var(--font-grey);
`;

export default NewTicketTitle;
