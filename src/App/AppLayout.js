import styled from "styled-components";

const FeaturesLayout = styled.main`
  width: 100%;
  min-width: 100vw;
  height: auto;
  min-height: 100vh;
  position: relative;
  background: var(--dark-grey);
`;

export default FeaturesLayout;
