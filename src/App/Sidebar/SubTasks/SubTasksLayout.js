import styled from "styled-components";

const SubTasksLayout = styled.div`
  margin-top: 24px;
  h4 {
    color: var(--orange);
  }
  ul {
    list-style: none;
    margin: 16px 0;
    cursor: pointer;
  }
  p {
    color: var(--orange);
  }
`;

export default SubTasksLayout;
