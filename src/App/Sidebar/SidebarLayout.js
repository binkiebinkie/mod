import styled from "styled-components";

const SidebarLayout = styled.div`
  width: 350px;
  background: var(--grey);
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  padding: 18px;
`;

export default SidebarLayout;
